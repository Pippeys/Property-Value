<html>
    <head>
        <title>My scratchboard page</title>
    </head>

    <body>

        <div>
            Please Type in your Expenses.
        </div>

        <div>
            <form action="." method="POST">
                <textarea name="contents" placeholder="Enter a value"></textarea>
                <input type="submit" value="Enter expenses">
            </form>
        </div>

        <div>
            Please Type in your number of units.
        </div>

        <div>
            <form action="." method="POST">
                <textarea name="contents" placeholder="Enter a value"></textarea>
                <input type="submit" value="Enter units">
            </form>
        </div>

        <div>
          Please Enter your Square Footage.
        </div>

        <div>
            <form action="." method="POST">
                <textarea name="contents" placeholder="Enter a comment"></textarea>
                <input type="submit" value="Enter Sqft">
            </form>
        </div>
        <div>
          Please enter the year your property was built.
        </div>

        <div>
            <form action="." method="POST">
                <textarea name="contents" placeholder="Enter a comment"></textarea>
                <input type="submit" value="Enter Year Built">
            </form>
        </div>
        <div>
          Please Enter your Zip Code.
        </div>

        <div>
            <form action="." method="POST">
                <textarea name="contents" placeholder="Enter a comment"></textarea>
                <input type="submit" value="Enter Zip Code">
            </form>
        </div>
    </body>
</html>
